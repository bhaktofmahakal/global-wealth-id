{
  "name": "global-wealth-id",
  "version": "1.0.0",
  "description": "Full-stack credit score converter",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "install-all": "npm -C backend install && npm -C frontend install",
    "dev": "concurrently \"npm -C backend run dev\" \"npm -C frontend run dev\"",
    "build": "npm -C backend install && npm -C frontend run build"
  },
  "engines": {
    "node": "18.x"
  },
  "devDependencies": {}
}
